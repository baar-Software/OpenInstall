{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the OpenInstall consent window. App-defined commands (resolve_oip, confirm_install) are not gated here; only core + plugin permissions are.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-ask"
  ]
}
